/**
 * CatalogIQ — Confirm Context
 *
 * Shared React context instance for app-wide confirmation dialogs.
 */

import { createContext } from "react";

export const ConfirmContext = createContext(null);
